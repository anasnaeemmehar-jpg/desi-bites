# In local dev, leave this unset — Vite's proxy (vite.config.js) handles /api and /uploads.
# In production (Vercel), set this to your deployed backend URL, e.g.:
# VITE_API_URL=https://desibites-backend.onrender.com
VITE_API_URL=
